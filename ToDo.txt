# ToDo.txt — RETIRED

This file previously described a centralized keyboard shortcut system.

That work is implemented:
- frontend/src/lib/keyboardShortcuts.ts
- Default shortcuts registered in frontend/src/lib/app/setup.ts
- Customization UI in Settings → Shortcuts (overrides via app settings)

Current improvement tracking lives in:

  fixit.txt

Do not treat this file as an active task list.
