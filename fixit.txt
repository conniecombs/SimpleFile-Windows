# SimpleFile-Windows — Improvement Scan (fixit)

Date: 2026-08-07
Source: docs/, build_notes/, and current code review.

This file captures improvement opportunities for SimpleFile-Windows.
Several older plan items are already done; the focus is on remaining work.

---

## Already solid (do not re-implement)

| Area | Evidence |
|---|---|
| Archive path safety (ZIP/TAR/RAR) | Marked complete in docs/IMPROVEMENT_PLAN.md |
| Windows filename validation | validate_name rejects reserved chars/devices, trailing `.` / space |
| Shared symlink copy | recreate_symlink used from fs_ops + progress |
| Huge-folder virtualization | List/grid windowing + lazy thumbnails/metrics |
| Shortcut registry + customization | keyboardShortcuts.ts, Settings → Shortcuts, overrides persist |
| Workspace layout restore | Tabs, dual-pane, paths, columns, preview, icon size |
| Renderer hardening | withGlobalTauri: false, modal HTML sanitization, guards |
| Windows-only packaging | NSIS/MSI only; android/ios/icns removed |

Note: `ToDo.txt` is retired (points here). `docs/IMPROVEMENT_PLAN.md` primary
sections are marked completed.

---

## Highest-value remaining improvements

### 1. Installer smoke in CI (release reliability) — DONE (2026-08-07)

Implemented `.github/workflows/installer-smoke.yml`:
- Manual `workflow_dispatch` + nightly schedule (06:00 UTC)
- Builds unsigned Windows NSIS/MSI via `npm run build:tauri:local`
- Runs `smoke:release`, `smoke:msi`, `smoke:installer`
- Uploads installer artifacts; workflow guard in `check-github-workflows.mjs`

Why (historical):
- Local scripts existed, but no CI packaging smoke job.
- Packaging breaks could ship undetected.

Docs:
- docs/IMPROVEMENT_PLAN.md §6
- docs/FEATURE_OPPORTUNITIES.md
- docs/ROADMAP.md

---

### 2. Replace DOM-ID workflow glue with component state — DONE (2026-08-07)

Completed migration slices:

1. **Progress modal** — `progressUi.svelte.ts` + `ProgressModal.svelte`
2. **Generic modal / Settings** — `modalUi.svelte.ts` + `GenericModal.svelte`
3. **Search + smart-folder chrome** — `searchUi.svelte.ts`, ToolbarShell bind
   for query/clear/cancel, ContentShell `SearchResultsHeader`, smart folder
   save via prompt dialog (no `#smart-folder-name` DOM read)
4. **Archive create/list** — `archiveUi.svelte.ts`, `ArchiveViewerModal.svelte`,
   `CreateArchiveModal.svelte` (static OverlayShell archive markup removed)
5. **About / keyboard-help** — `aboutUi.svelte.ts` + `AboutModal.svelte`,
   `keyboardHelpUi.svelte.ts` + `KeyboardHelpModal.svelte` (static OverlayShell
   markup removed; About no longer uses HTML dialog path)
6. **Advanced rename form fields** — `advancedRenameUi.form` with bind:checked /
   bind:value; plan generation via `formChecked` / `formString` (no DOM reads)

Why (historical):
- Controllers drove overlays via getElementById on OverlayShell markup.

---

### 3. Richer transfer progress messaging — DONE (2026-08-07)

Implemented richer transfer progress in the component-owned progress UI:

- Size preflight ("Calculating size…") so totals are known early
- Bytes copied / total, smoothed rate (B/s–TB/s), and ETA
- Indeterminate bar while preparing (total still unknown)
- Cancel keeps the dialog open with **Cancelling…** until the backend
  acknowledges or a safety timeout fires
- Current item shows basename (full path in tooltip)

Key files:
- `frontend/src/lib/app/progressUi.svelte.ts`
- `frontend/src/lib/components/ProgressModal.svelte`
- `frontend/src/css/modules/modal.css`
- `src-tauri/src/progress.rs` (estimate + cancel-friendly walk)

---

### 4. Dual-pane keyboard workflow polish — DONE (2026-08-07)

Implemented dual-pane keyboard and chrome polish:

| Shortcut | Action |
|---|---|
| F6 | Toggle dual pane |
| Tab | Switch active pane (when dual pane on) |
| Alt+1 / Ctrl+Shift+Left | Focus left pane |
| Alt+2 / Ctrl+Shift+Right | Focus right pane (enables dual pane if needed) |
| Ctrl+Alt+C | Copy selection to other pane |
| Ctrl+Alt+M | Move selection to other pane |

Also:
- Active pane inset accent + header tint in dual-pane mode
- Click pane chrome to activate
- Status bar shows **Left pane** / **Right pane** + active path
- Help modal + Settings shortcut list include Dual Pane section

---

### 5. Metadata depth (preview / properties) — DONE (2026-08-07)

Implemented unified `get_file_metadata` for PDF / audio / video (MP4 family) /
Office packages, with size caps. Properties panel shows Details + Metadata
grids; image EXIF path retained.

Why (historical):
- metadata.rs was mainly image EXIF.
- Opportunities doc listed richer media/document panels.

Incremental path completed:
- PDF page count / title
- Audio tags
- Video dimensions/duration (MP4/M4V/MOV)
- Office basic props
- Size/time limits so large files stay safe

---

### 6. Mapped network drive error UX — DONE (2026-08-07)

Implemented clearer mapped-network UX:

**Backend (`drives.rs`)**
- 3s timeout on network probe so dead maps no longer hang drive listing
- Timeout → `offline` with explicit “timed out / check VPN / retry” detail
- Existing Windows errors still map to offline/stale with actionable copy

**Frontend**
- Offline / Stale badges on My PC tree roots
- Richer descriptions (access denied, not ready, timed out, share path)
- My PC **refresh drives** control (↻) re-probes mappings
- Opening an offline mapped drive shows reconnect dialog with Retry
- Failed open refreshes drive status so badges stay current

Key files:
- `src-tauri/src/drives.rs`
- `frontend/src/lib/components/layout-shell/SidebarShell.svelte`
- `frontend/src/lib/components/tree-view/TreeView.svelte`
- `frontend/src/lib/app/core.ts` (`refreshDrives`, reconnect dialog)
- tree/sidebar/modal CSS

---

## Medium priority (code health)

| Improvement | Notes |
|---|---|
| Tighten TypeScript `any` | **Done:** typed setup event details; ColorLabelTag on tag APIs/appState |
| Unify tab persistence | **Done:** workspace layout only; legacy simplefile-tabs migrates then clears |
| Refresh stale planning docs | **Done:** ToDo.txt retired; IMPROVEMENT_PLAN primary items marked complete |
| Folder-metrics progress | **Done:** per-folder progress line, cumulative size, cancel keeps partial results |

---

## Product / release (from roadmap)

- Broader smoke: settings startup, updater metadata, MSI, NSIS install/uninstall
- Targeted tests for mapped-drive display names
- Keep archive validation tight as formats grow
- Expand metadata carefully with preview limits

Out of scope (intentionally):
- Cloud providers
- Linux/macOS packaging

---

## Suggested next work order

1. ~~Installer smoke workflow~~ — done
2. ~~Transfer progress (bytes / speed / ETA)~~ — done
3. ~~Progress + generic modal + search/archive off DOM IDs~~ — done (item 2)
4. ~~Dual-pane keyboard focus~~ — done
5. ~~Metadata expansion~~ — done (properties panel)
6. ~~Mapped network drive error UX~~ — done
7. ~~Type cleanup + doc sync~~ — done
8. ~~Advanced rename / Quick Look component ownership~~ — done
9. ~~Folder-metrics progress polish~~ — done

---

## Candidate next implementations

### Drag / marquee multi-select — DONE (2026-08-07)

Rubber-band selection for files and folders:

- Drag empty space in the file list (list or grid) to select intersecting items
- Ctrl/Meta + drag adds to the existing selection
- Click empty space clears selection
- Auto-scroll near top/bottom edges while dragging
- Works with virtualized huge folders (geometry hit-test, not DOM-only)
- Does not conflict with item drag-and-drop (starts only off `.file-item`)

Key files:
- `frontend/src/lib/marqueeSelection.ts`
- `frontend/src/lib/components/file-list/FileList.svelte`
- `frontend/src/css/modules/interactions.css` (`.selection-rect`)
- `frontend/scripts/check-marquee-selection.mjs`

---

Pick a new product slice from docs/FEATURE_OPPORTUNITIES.md or docs/ROADMAP.md.

---

## Key paths reference

Frontend:
- frontend/src/main.ts — app entry
- frontend/src/App.svelte — shell
- frontend/src/lib/components/ — Svelte UI
- frontend/src/lib/app/ — workflow orchestration
- frontend/src/lib/api.ts, frontend/src/lib/tauri.ts — Tauri boundary
- frontend/src/lib/keyboardShortcuts.ts — shortcut registry
- frontend/src/vanilla-js/runtime/state.svelte.ts — layout/tabs persistence
- frontend/src/lib/components/OverlayShell.svelte — progress/modal host

Backend:
- src-tauri/src/fs_ops.rs — filesystem ops
- src-tauri/src/progress.rs — progress-aware transfers
- src-tauri/src/drives.rs — Windows drives / mapped shares
- src-tauri/src/archive.rs — archives
- src-tauri/src/metadata.rs — image/EXIF metadata
- src-tauri/src/preview.rs — previews / open / reveal
- src-tauri/src/utils.rs — validate_name, recreate_symlink

Docs / notes:
- docs/IMPROVEMENT_PLAN.md
- docs/FEATURE_OPPORTUNITIES.md
- docs/ROADMAP.md
- docs/BUGS.md
- docs/UI_BACKEND_REVIEW.md
- build_notes/

Commands:
- npm run check
- npm run check:rust
- npm run check:security
- npm run build:tauri:local
- npm run smoke:settings
- npm run smoke:release
- npm run smoke:msi
- npm run smoke:installer
