[package]
name = "simplefile"
version = "1.1.0"
description = "A simple file explorer built with Tauri"
authors = ["SimpleFile Team"]
license-file = "../LICENSE"
repository = "https://github.com/conniecombs/SimpleFile-Windows"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
tauri-plugin-updater = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4"
notify = { version = "8", features = ["serde"] }
trash = "5"
md-5 = "0.11"
sha1 = "0.11"
tokio = { version = "1", features = ["sync", "rt", "rt-multi-thread", "macros", "fs", "io-util", "net"] }
parking_lot = "0.12"
base64 = "0.23"
image = { version = "0.25", default-features = false, features = ["jpeg", "png", "gif", "webp", "bmp"] }
zip = { version = "8", default-features = false, features = ["bzip2", "deflate64", "deflate", "lzma", "ppmd", "time", "zstd", "xz"] }
flate2 = "1"
tar = "0.4"
unrar = "0.5"
walkdir = "2"
filetime = "0.2"

futures = "0.3"
log = "0.4"
glob = "0.3"
once_cell = "1"
getrandom = "0.4"
reqwest = { version = "0.13", default-features = false, features = ["rustls"] }


# Used by checksum and disk cleanup code for streaming SHA-256 hashes.
sha2 = "0.11"

# Pull in the `exif` crate for reading EXIF metadata from image files. This
# small crate has no heavy dependencies and supports common image formats.
exif = { package = "kamadak-exif", version = "0.6" }
# PDF document info / page counts for the properties panel.
lopdf = { version = "0.44", default-features = false }
# Audio tags and duration without native FFmpeg dependencies.
lofty = "0.24"
tauri-plugin-dialog = "2"
rusqlite = { version = "0.39.0", features = ["bundled"] }

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["accctrl", "aclapi", "errhandlingapi", "fileapi", "handleapi", "minwinbase", "minwindef", "winbase", "winerror", "winnetwk", "winnt"] }

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
