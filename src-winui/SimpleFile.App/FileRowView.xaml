<UserControl
    x:Class="SimpleFile.App.FileRowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleFile.App">
    <Grid x:Name="RowGrid" ColumnSpacing="8" MinHeight="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="NameColumn" Width="*" />
            <ColumnDefinition x:Name="SizeColumn" Width="100" />
            <ColumnDefinition x:Name="DateColumn" Width="160" />
            <ColumnDefinition x:Name="TypeColumn" Width="100" />
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBlock x:Name="IconText" VerticalAlignment="Center" FontFamily="Segoe MDL2 Assets" FontSize="16" />
            <TextBlock
                x:Name="NameText"
                VerticalAlignment="Center"
                TextTrimming="CharacterEllipsis" />
        </StackPanel>
        <TextBlock
            x:Name="SizeText"
            Grid.Column="1"
            VerticalAlignment="Center"
            Foreground="{ThemeResource SfTextMutedBrush}" />
        <TextBlock
            x:Name="DateText"
            Grid.Column="2"
            VerticalAlignment="Center"
            Foreground="{ThemeResource SfTextMutedBrush}" />
        <TextBlock
            x:Name="TypeText"
            Grid.Column="3"
            VerticalAlignment="Center"
            Foreground="{ThemeResource SfTextMutedBrush}" />
    </Grid>
</UserControl>
